[build]
  command = "npm run build"
  publish = "dist"

# SPA: send all routes to index.html so React Router works (e.g. /booking direct loads)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
